{
  "name": "tech-stack-showcase",
  "version": "1.0.0",
  "description": "A tech stack showcase website — managed via Airtable, built with Handlebars, deployed on Netlify",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node dev-server.js",
    "init-content": "node scripts/init-content.js",
    "sync:push": "node scripts/sync-airtable.js push",
    "sync:pull": "node scripts/sync-airtable.js pull",
    "sync:status": "node scripts/sync-airtable.js status",
    "lint": "npx @biomejs/biome check .",
    "lint:fix": "npx @biomejs/biome check . --write"
  },
  "keywords": [
    "tech-stack",
    "portfolio",
    "airtable",
    "static-site",
    "netlify"
  ],
  "author": "Moana",
  "license": "MIT",
  "dependencies": {
    "chokidar": "^3.6.0",
    "express": "^4.22.1",
    "fs-extra": "^11.3.3",
    "handlebars": "^4.7.8",
    "markdown-it": "^14.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4"
  }
}
